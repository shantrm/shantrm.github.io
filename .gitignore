# Supabase config - NOTE: The anon key is safe to commit (it's public by design)
# Only keep this in .gitignore if you want to use a different config locally
# For GitHub Pages, you should commit config.js with your anon key
# config.js

# Profanity wordlist (contains explicit content)

# Node modules (if you use the Express server)
node_modules/
api/node_modules/

# Environment variables
.env
.env.local

# Logs
*.log
npm-debug.log*

# OS files
.DS_Store
Thumbs.db

